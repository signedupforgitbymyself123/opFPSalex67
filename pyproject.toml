[project]
name = "alexgameop"
version = "0.1.0"
description = "Simple pygame starter game"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "pygame>=2.5.0",
]

[tool.uv]
package = false
